{
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "ghooks": "^1.2.1",
    "lerna": "2.0.0-beta.30"
  },
  "config": {
    "ghooks": {
      "pre-commit": "lerna run lint && lerna run test"
    }
  }
}
